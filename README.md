# Customer-Behaviour-Analysis-Dashboard
This project analyzes customer shopping behavior using SQL, Python, and Power BI to generate actionable business insights. The objective is to transform raw retail transaction data into meaningful insights through data cleaning, exploration, querying, and interactive visualization.

The project demonstrates an end-to-end data analytics workflow — from raw dataset to dashboard reporting.
